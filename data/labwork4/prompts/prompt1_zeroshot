
You receive input data X (TypeScript project source files) and must produce output y (analysis results) by completing all stages of the pipeline: data understanding, semantic inference, reasoning, and structured output generation.

### Task:
Given the following input X, perform full dependency analysis and return y.

### X (Input):
Project: "MiniMathTS"
Source files:
-----------------------------
/index.ts
import { add } from "./utils/add";
import { subtract } from "./utils/subtract";
export const result = add(10, subtract(8, 3));
-----------------------------
/utils/add.ts
export function add(a: number, b: number) { return a + b; }
-----------------------------
/utils/subtract.ts
export function subtract(a: number, b: number) { return a - b; }
-----------------------------

### Expected Output Format (y):
Return results in structured form containing:
1) Dependency Graph (text or JSON)
2) DSM (Design Structure Matrix)
3) Circular dependency detection results
4) Short developer-friendly summary

Begin analysis and produce y.

