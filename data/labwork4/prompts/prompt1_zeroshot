You are an AI agent for analyzing TypeScript project dependencies.
Perform the full analysis pipeline (AST parsing → semantic analysis → dependency extraction → DSM → report generation).

### X (Source Code Input)
Project name: "CircleTS"

Files:
-----------------------------
/A.ts
import { funcB } from "./B";
export const funcA = () => funcB();
-----------------------------
/B.ts
import { funcC } from "./C";
export const funcB = () => funcC();
-----------------------------
/C.ts
import { funcA } from "./A";
export const funcC = () => funcA();
-----------------------------

### Output y format required:
1) Dependency Graph (JSON)
2) DSM (Design Structure Matrix)
3) Circular dependency findings
4) Structural report with improvement suggestions

Start analysis and return y.

Output:
1) Dependency Graph (JSON)
{
  "nodes": ["A", "B", "C"],
  "edges": [
    { "from": "A", "to": "B", "type": "import" },
    { "from": "B", "to": "C", "type": "import" },
    { "from": "C", "to": "A", "type": "import" }
  ]
}

2) DSM (Design Structure Matrix)
      A   B   C
A     0   1   0
B     0   0   1
C     1   0   0

3) Circular Dependency Findings
A ↔ B ↔ C form a 3-way circular dependency cycle.
The system cannot determine a stable execution order because each module depends on another module that depends back on it.

4) Structural Report
- Graph density: High (3 edges among 3 modules)
- Coupling Level: Strong cyclic coupling
- Architectural Risk: High

5) Improvement Suggestions
- Extract shared functionality from `C.ts` into a new module (e.g., Core.ts)
- Replace direct mutual imports with interface-based dependency injection or event callbacks
- Introduce a top-level orchestrator that coordinates A, B, and C instead of linking them in a chain
